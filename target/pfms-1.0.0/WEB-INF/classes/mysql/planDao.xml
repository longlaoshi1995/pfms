<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.pfms.dao.IPlanDao" >

    <select id="getMonthPlan"  parameterType="String"  resultType="java.util.HashMap">
        SELECT a.month_time,b.category_name,a.expense  from  plan_month a, bill_category b WHERE   month_time = '${_parameter}' and  a.category_id =b.id;
    </select>

</mapper>