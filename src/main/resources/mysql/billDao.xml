<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.pfms.dao.BillDao" >


    <select id="getCategory"  resultType="java.util.HashMap">
        select * from bill_category      ;
    </select>

    <select id="getMonthBill"  parameterType="String"  resultType="java.util.HashMap">
        SELECT a.month_time,b.category_name,a.expense  from  bill_month a, bill_category b WHERE   month_time = '${_parameter}%' and  a.category_id =b.id;
    </select>

    <select id="getYearBill"  parameterType="String"  resultType="java.util.HashMap">
        SELECT  month_time as month,SUM(expense)  as  num from bill_month  WHERE month_time like '${_parameter}%'  GROUP BY month_time;
    </select>
</mapper>